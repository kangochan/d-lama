<nav class="header navbar navbar-default">
      <div class="container-fluid">
        <div class="row">
          <div class="col-md-10 col-md-offset-1">
            <!-- Brand and toggle get grouped for better mobile display -->
            <div class="navbar-header">
              <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
              </button>
              <a class="navbar-brand" href="/">Dlama</a>
            </div>

            <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
              <form class="navbar-form navbar-left" role="search">
              <% if user_signed_in? %>
                <div class="form-group">
                  <input type="text" class="form-control" placeholder="映画、キャストなどを検索" size="30">
                </div>
                <button type="submit" class="btn btn-default">検索</button>
              <% end %>
              </form>
              <ul class="nav navbar-nav navbar-right">
                <% if user_signed_in? && current_user.published? %>
                  <li>
                    <%= link_to "作品投稿", new_product_path %>
                  </li>
                <% end %>
              <% if user_signed_in? %>
                <li><a href="#">画像</a></li>
                <li class="dropdown">
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><span class="username"><%= current_user.nickname %></span><span class="caret"></span></a>
              <% else %>
                <li><%= link_to "アカウント作成", new_user_registration_path, class: 'post' %></li>
                <li><%= link_to "ログイン", new_user_session_path, class: 'post' %></li>
              <% end %>
                  <ul class="dropdown-menu" role="menu">
                    <% if user_signed_in? %>
                      <li>
                        <%= link_to "マイページ", user_path(current_user) %>
                      </li>
                      <li class="entry-button">
                        <%= link_to 'サインアウト', destroy_user_session_path, method: :delete %>
                      </li>
                      <li>
                        <%= link_to "プロフィール編集", edit_user_path(current_user) %>
                      </li>
                    <% end %>
                  </ul>
                </li>
              </ul>
            </div><!-- /.navbar-collapse -->
          </div>
        </div>
      </div><!-- /.container-fluid -->
    </nav>
